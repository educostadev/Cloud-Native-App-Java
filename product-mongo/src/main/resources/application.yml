server:
  port: 8085

management:
  security:
    enabled: false

eureka:
  instance:
    lease-renewal-interval-in-seconds: 5

logging:
  level:
    ROOT: INFO

spring:
  data:
    mongodb:
      database: masterdb
      username: mongoadmin
      password: mongopwd
      authentication-database: admin
  activemq:
    broker-url: tcp://localhost:61616
  jms:
    pub-sub-domain: true

custom:
  product-topic: ProductT
